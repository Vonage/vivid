{
  "color": {
    "base": {
      "background": { "value": "{alias.color.scheme.base.background.value}" },
      "foreground": { "value": "{alias.color.scheme.base.foreground.value}" },
      "dim": { "value": "{alias.color.scheme.base.dim.value}" }
    },
    "surface": {
      "background": {
        "value": "{alias.color.scheme.surface.background.value}"
      },
      "foreground": { "value": "{alias.color.scheme.surface.foreground.value}" }
    },
    "primary": {
      "background": { "value": "{alias.color.scheme.primary.background.value}" },
      "foreground": { "value": "{alias.color.scheme.primary.foreground.value}" }
    },
    "secondary": {
      "background": { "value": "{alias.color.scheme.secondary.background.value}" },
      "foreground": { "value": "{alias.color.scheme.secondary.foreground.value}" }
    }
  }
}
