/* eslint-disable no-shadow */
export enum Scheme {
	LIGHT = 'light',
	DARK = 'dark',
}

// export enum Auto {
// 	SYNC_WITH_OS_SETTINGS = 'auto',
// }

// export type Option = OptionScheme | OptionAuto;

// export interface SelectedDetail {
// 	scheme: Option;
// }
