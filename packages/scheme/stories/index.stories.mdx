import { Story, Preview, Meta, Props, html } from '@open-wc/demoing-storybook';

// import '../vvd-scheme.js';
import '../../button/vwc-button.js';

<Meta
  title="VividScheme"
  parameters={{
    component: 'vwc-scheme'
  }}
/>

# VividSchemeLoader

A component for automatically load user prefered scheme (currently light/dark)

## Features:

- a
- b
- ...

## How to use

<Preview withToolbar>
  <Story name="Simple">
    {html`
      <vwc-button>Button</vwc-button>
    `}
  </Story>
</Preview>

### Installation

```bash
yarn add vwc-scheme
```

place module reference in the head of your html to apply vivid styling for your application and to the vivid web components
either by a plain old script tag (with no defer/async).
or by the following 'modulepreload' approach where supported
our goal is to make sure the script gets high priority

```
<script rel="modulepreload" src="../node_modules/@vonage/scheme/scheme.js"></script>
```

preloading the scheme is important as it is a critical styling resource

```
<script type="module" src="../node_modules/@vonage/scheme/scheme.js"></script>
```
