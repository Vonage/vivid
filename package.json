{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start:volta": "lerna run start --scope @vonage/volta",
    "build:volta": "lerna run build --scope @vonage/volta"
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
